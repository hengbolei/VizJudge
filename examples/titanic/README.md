# Titanic example

Download a Titanic CSV that you are licensed to use, then run:

```bash
vizjudge analyze titanic.csv --target survived --output ../../outputs/titanic
```

Expected high-value evidence includes passenger class, fare, sex, and age relationships with
the target. Treat this as a smoke test, not a performance benchmark.

